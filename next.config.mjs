/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "images.unsplash.com",
      "storage.googleapis.com/pase-achi/team-images/",
    ],
  },
};

export default nextConfig;
